// import type { Shape } from "../types/Shape"

// export function moveShape(
//   shape: Shape,
//   original: Shape,
//   dx: number,
//   dy: number,
// ) {
//   switch (shape.type) {
//     case 'rectangle':
//       shape.x = original.x + dx
//       shape.y = original.y + dy
//       break

//     default:
//       console.warn(`Cannot move unknown shape type: ${shape.type}`)
//   }
// }